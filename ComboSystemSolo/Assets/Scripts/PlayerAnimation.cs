using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SocialPlatforms;

public class PlayerAnimation : MonoBehaviour
{
    // Start is called before the first frame update

    //public AnimationManager.Character character = AnimationManager.Character.MAGNETO;
    //public AnimationManager.State state = AnimationManager.State.IDLE;

    //[SerializeField][Range(0, 0.3f)]
    //float delay = 0.3f;

    //float count = 0.0f;

    //void Start()
    //{
    //    count = delay;
    //    AnimationManager.Instance().GetNextAnimation(this.gameObject);
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    count -= Time.deltaTime;

    //    if (count <= 0.0f)
    //    {
    //        AnimationManager.Instance().GetNextAnimation(this.gameObject);
    //        count = delay;
    //    }
    //}
}
